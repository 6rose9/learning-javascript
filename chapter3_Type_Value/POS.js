let item = "Apple";
let qty = 10;
let price = 15.0;

let total = qty * price;
console.log(`Total cost of ${qty} ${item} is \$${total}`);

let item1 = {
    name: "Banana",
    qty: 5,
    price: 11.5,
}

console.log(`Total cost of ${item1.qty} ${item1.name} is \$${item1.qty * item1.price}`);